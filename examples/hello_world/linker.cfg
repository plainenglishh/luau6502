MEMORY {
    ROM: start = $0000, size = $100, type = ro, fill = yes;
}

SEGMENTS {
    CODE: load = ROM, start = $00, type = ro, define = yes;
    VECTORS: load = ROM, start = $FC, type = ro, define = yes;
}